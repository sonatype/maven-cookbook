<?xml version="1.0"?>
<project name="create-deps">
  
  <target name="create">
    <echo file="${outputDir}/deps.txt"
	  message="compile classpath: ${compile_classpath}"/>
    
    <copy todir="${outputDir}">
      <fileset dir="${sourceDir}"/>
    </copy>
    
  </target>

</project>
